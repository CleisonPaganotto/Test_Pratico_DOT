{
  "name": "demoblaze-tests",
  "version": "1.0.0",
  "description": "Automação do fluxo de compra no Demoblaze com Cypress",
  "scripts": {
    "test": "cypress run --env allure=true",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.1",
    "allure-commandline": "^2.34.0",
    "cypress": "^13.0.0"
  }
}
